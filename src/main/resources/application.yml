
spring:
  main:
    keep-alive: true
  threads:
    virtual:
      enabled: true
  data:
    redis:
      cluster:
        nodes:
          - localhost:6379
        max-redirects: 4
      timeout: 10000
  application:
    name: spring-redis-playground


server:
  port: 8080